# JSON/YAML providers

There is an abstraction layer above JSON structure that allows usage with different JSON libraries.
There are just two interfaces in this layer:
 - [JsonNode](https://javadoc.io/doc/dev.harrel/json-schema/latest/dev/harrel/jsonschema/JsonNode.html) - 

todo:
 - desc of adapter/wrapper classes and contracts
 - link to package javadoc .providers
 - info + link about adding custom providers
 - only 1 provider should be used in most cases, 2 providers mostly for json+yaml